#include<stdio.h>
#include<conio.h>

int main()
{
char n[100];
int i,j,temp=0,palindrome=0,d,sum=0;
printf("enter the number");
gets(n);
i=0;
strlen(n);
for(i=0;n[i]='\0';i++)
while(i!='\0')


    strrev(n);

printf("the palindrome is %d",n);
return 0;
}
